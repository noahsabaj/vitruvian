# Session — 2026-04-16

**Duration:** ~2 hours (evening).
**Focus:** Project kickoff. Scope, architecture, tooling decisions, paper
trail.

---

## What happened

- Imported context from a prior web-Claude conversation covering: the case
  for humanoid as MI substrate, MVH mechanical spec, DIY vs buy parts
  breakdown, simulation-first software roadmap, and the project name
  ("Vitruvian").
- Audited local rig: RTX 4060 Ti (8 GB), i7-14700F, 31 GiB RAM, Linux
  Mint 22.3, CUDA 12.8 toolkit, Python 3.12, `uv` installed, Docker
  present, `conda` present but unused by this project.
- Planning discussion covering environment manager, repo structure,
  milestone definition, newborn-vs-foundation-model framing, cadence,
  experiment tracking, git hosting, license.

## Decisions locked

See `docs/decisions/`.

1. **ADR 001** — `uv` for Python environment.
2. **ADR 002** — Monorepo with vendored submodules.
3. **ADR 003** — Graduated Vitruvian: frozen priors + plastic world model.
   Self-learning is the plastic core.
4. **ADR 004** — wandb for experiment tracking.
5. **ADR 005** — Apache 2.0 license.

Additional commitments:

- Milestone 1 scoped as "Unitree G1 walks in MJX via PPO, reproducibly,
  on local GPU, video + wandb run preserved."
- Sub-milestones M0.1 → M0.4 defined for pipeline verification before M1.
- Simulation-first. Hardware is Phase 5.
- Docs structure: `docs/roadmap.md`, `docs/thesis.md`, `docs/decisions/`,
  `docs/journal/`.
- Cadence: regular (evenings / weekends), commit + journal entry each
  session.
- GitHub, private, for now.

## Artifacts produced

- `README.md`
- `LICENSE` (Apache 2.0)
- `.gitignore`
- `docs/roadmap.md`
- `docs/thesis.md`
- `docs/decisions/001` through `005`
- `docs/journal/2026-04-16.md` (this file)
- `pyproject.toml`, `.python-version`, `uv.lock`, `src/vitruvian/__init__.py`
- `tests/.gitkeep`, `scripts/.gitkeep`, `external/README.md`

## M0.1 — executed in same session

- `uv init --package --python 3.12 --vcs none` produced `pyproject.toml`,
  `.python-version` (pinned 3.12), `src/vitruvian/__init__.py`.
- `pyproject.toml` updated with `readme` and `license = { file = "LICENSE" }`.
- SPDX header added to `src/vitruvian/__init__.py`.
- Scaffolding: `tests/`, `scripts/`, `external/` (with its own README)
  created.
- `uv sync` succeeded; `.venv` built against system CPython 3.12.7
  (anaconda's), `uv.lock` generated and committed.
- `git init -b main`, initial commit of 18 files / 890 insertions.
- `gh repo create vitruvian --private --source=. --push` — repo live at
  https://github.com/noahsabaj/vitruvian, `main` tracks `origin/main`.

**Status:** M0.1 complete.

## Next session — M0.2

1. Add `mujoco`, `mujoco-mjx`, `jax[cuda12]`, `brax` as project deps via
   `uv add`.
2. Smoke test: `python -c "import jax; print(jax.devices())"` — confirm
   the RTX 4060 Ti is visible to JAX.
3. Add `mujoco_menagerie` as a git submodule under `external/`.
4. Open the MuJoCo viewer and load Unitree G1 manually — confirm the
   MJCF parses and renders (this bleeds into M0.3).

Watch items:
- JAX's `jax[cuda12]` wheel expects CUDA 12.x — we have 12.8, should be
  clean. If it complains about cuDNN, note the version it wants.
- The 8 GB VRAM limit is not binding at M0.2 but will be once we start
  running parallel MJX envs at scale.

## Open questions carried forward

- Which visual encoder in Phase 3: V-JEPA 2, DINOv3, or other?
- At what point does language enter the stack, and how?
- Which self-learning signal to prioritize in Phase 4: RND vs
  Plan2Explore vs empowerment?
- When (if ever) does a "frozen" prior become unfrozen, and on what
  signal?
